# ==========================================
# 1. LOCAL EMULATOR CONFIGURATION (Optional)
# ==========================================
# Uncomment to use local Firestore emulator instead of production Cloud Firestore:
# FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"

# ==========================================
# 2. FIREBASE FIRESTORE CONFIGURATION
# ==========================================
# All Firestore access is server-side via firebase-admin — there is no
# client-side Firebase SDK in this project, so no NEXT_PUBLIC_FIREBASE_*
# variables are needed.
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxx@your-project-id.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Optional alternative to the credentials above:
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"

# ==========================================
# 3. BETTER-AUTH & SESSION CONFIGURATION
# ==========================================
# Required. Better Auth signs session tokens with this — without it, sessions
# become forgeable. Generate one with:
#   openssl rand -base64 32
BETTER_AUTH_SECRET="a_random_32_character_secret_key"
BETTER_AUTH_URL="http://localhost:3000"  # Set to production URL when deployed

# ==========================================
# 4. GOOGLE OAUTH PROVIDER (VIT Email Auth)
# ==========================================
GOOGLE_CLIENT_ID="xxxx-xxxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxxx"

# ==========================================
# 5. APPLICATION WINDOW
# ==========================================
# Optional. Once this date has passed, /api/submit-form rejects new
# applications. Leave unset for no deadline. Any format Date() accepts.
APPLICATION_DEADLINE="2026-09-30T23:59:59+05:30"

# ==========================================
# 6. NODEMAILER EMAIL BROADCAST (Optional)
# ==========================================
EMAIL_USERNAME="your-email@gmail.com"
EMAIL_PASSWORD="your-gmail-app-password"
